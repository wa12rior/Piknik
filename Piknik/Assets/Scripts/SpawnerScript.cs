using UnityEngine;
using System.Collections;

public class SpawnerScript : MonoBehaviour {
/*
	public GameObject[] spawners;

	public int[] intensity;
	public int currentScore;

	private bool t = true;
	private bool f = false;

	public void SpawnWave() {
		

	}

	public void SpawnEntity(Transform position) {
		Instantiate(target, new Vector3(DistanceX, DistanceY, 0), Quaternion.identity);
	}
	
	void Start() {
		InvokeRepeating(SpawnWave());

	}

	public void levelLoop() {
		for (int i = 1; i <= 20; i++) {
			intensity[i] = i * 10; 
		}

	}

	public void activityLoop(bool z, bool x, bool c, bool v) {
			spawners[0].SetActive(z);
			spawners[1].SetActive(x);
			spawners[2].SetActive(c);
			spawners[3].SetActive(v);

	}

	public void intensityInstantiate() {
		if(currentScore <= 10) {
			activityLoop(t, f, f, f);
		} 
		else {
			activityLoop(f, t, t, t);
		}

	}

	void Start() {
		currentScore = 0;
	}

	void Update() {
		currentScore = PlayerScript.score;

		intensityInstantiate();


	}

*/
}